# ── Core ML & Training ──────────────────────────────────
torch>=2.1.0
torchvision>=0.16.0
timm>=0.9.12           # EfficientNet & other pretrained models
ultralytics>=8.0.0     # YOLOv8 for instance segmentation

# ── API & Backend (FastAPI) ───────────────────────────
fastapi>=0.100.0
uvicorn>=0.23.0
python-multipart>=0.0.6 # For handling file uploads
pydantic>=2.0.0

# ── Data & Augmentation ───────────────────────────────
albumentations>=1.3.1
opencv-python>=4.8.0
Pillow>=10.0.0
numpy>=1.24.0
pandas>=2.0.0
scikit-learn>=1.3.0

# ── Utilities ─────────────────────────────────────────
tqdm>=4.66.0
pyyaml>=6.0
matplotlib>=3.7.0
seaborn>=0.12.0

# ── Dataset Download ──────────────────────────────────
kaggle>=1.5.16         # for TrashNet via Kaggle API
requests>=2.31.0
gdown>=4.7.1           # Google Drive download (TACO dataset)

# ── Optional Extras ───────────────────────────────────
# tensorboard>=2.14.0  # Optional: training visualization
# wandb>=0.16.0        # Optional: Weights & Biases
sqlalchemy>=2.0.0
